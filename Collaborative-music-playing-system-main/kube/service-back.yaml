apiVersion: v1
kind: Service
metadata:
  name: my-service-back
  labels:
    namespace: dna-music
spec:
  type: ClusterIP
  selector:
      app: backend
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP